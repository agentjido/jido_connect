# Changelog

## 0.8.0

- Add the distinct Bitbucket Cloud provider identity.
- Add the reviewed `bitbucket.pull_request.list` reader action.
- Add strict pull-request response normalization and a read-only catalog pack.
