defmodule Jido.Connect.Calendly.Client do
  @moduledoc "Calendly API client boundary."

  # Client API methods will be delegated to focused sub-modules as
  # capability areas are added in subsequent scaffold steps.
end
